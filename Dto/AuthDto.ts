class Auth {
    nombres: string;
    password: string
    constructor(
        nombres: string,
        password: string
    ) {
        this.nombres = nombres;
        this.password = password
    }
}

export default Auth;